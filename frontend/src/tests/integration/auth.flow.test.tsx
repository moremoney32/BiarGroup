// Tests d'intégration flow auth — à implémenter avec MSW
// import { render, screen, fireEvent, waitFor } from '@testing-library/react'
// import { setupServer } from 'msw/node'
// import { rest } from 'msw'

describe('Auth flow', () => {
  it('login redirige vers /dashboard', async () => {
    // TODO: implement with MSW mock server
  })

  it('logout redirige vers /login', async () => {
    // TODO: implement
  })

  it('accès route protégée sans auth redirige vers /login', async () => {
    // TODO: implement
  })
})
