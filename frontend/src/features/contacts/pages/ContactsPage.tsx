import PageTransition from '../../../components/animations/PageTransition'

export default function ContactsPage() {
  return (
    <PageTransition>
      <div className="space-y-6">
        <h1 className="text-xl font-semibold text-white">Contacts — CRM</h1>
        {/* TODO: liste contacts, import/export CSV, listes, tags, segmentation */}
      </div>
    </PageTransition>
  )
}
