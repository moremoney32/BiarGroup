import PageTransition from '../../../components/animations/PageTransition'

export default function WhatsappPage() {
  return (
    <PageTransition>
      <div className="space-y-6">
        <h1 className="text-xl font-semibold text-white">B-WHATSAPP — WhatsApp Business</h1>
        {/* TODO: campagnes bulk, templates, chatbot IA, contacts */}
      </div>
    </PageTransition>
  )
}
